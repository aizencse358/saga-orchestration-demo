package com.sagademo.temporal;

class OrchestratorTemporalApplicationTests {

    // Context-load test intentionally omitted: this module requires a live Temporal server
    // to start (spring.temporal.connection.target), which isn't available in a plain unit-test
    // context. Verification for this module is done via docker compose, see project README.
}
